#pragma once

namespace moth_editor {
    inline constexpr int VersionMajor = 1;
    inline constexpr int VersionMinor = 0;
    inline constexpr int VersionPatch = 0;
    /// Full version string including any pre-release suffix (e.g. "1.0.0-rc.1").
    inline constexpr char const* Version = "1.0.0-rc.1";
}
